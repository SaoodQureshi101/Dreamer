import { config } from "../config.js";

export const getCostOfLivingFromTeleport = async (city) => {
  // API disabled due to DNS/network issues
  return { score_out_of_10: 10.0 };
};

export const getHousingFromTeleport = async (city) => {
  // API disabled due to DNS/network issues
  return 400000;
};